#include "test.h"

int main(int argc, char **argv)
{ 
/*
	if (argc != 2)
	{
		printf("%s numRec\n", argv[0]);
		return -1;
	}
	int numRec = atoi(argv[1]);
*/
// 	testStream();
	testFor();
//	testDelta();
//	testNS();
//	testRLE(numRec);
        return 0;
}
